# Copy this file to .env and fill in your own values. Never commit .env.

ANTHROPIC_API_KEY=

# Paper-trading credentials from https://app.alpaca.markets/paper/dashboard/overview
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
APCA_BASE_URL=https://paper-api.alpaca.markets

# Free-tier key from https://finnhub.io — used for real earnings-date lookups
# instead of inferring earnings timing from news headlines.
FINNHUB_API_KEY=

# Free-tier key from https://resend.com — used to email full report content.
RESEND_API_KEY=
REPORT_TO_EMAIL=

# Set to 1 only for unattended/scheduled runs (e.g. the Trading Session cloud
# routine) — auto-approves orders instead of prompting, but requires limit
# orders and enforces the caps below. Leave unset for interactive/manual use.
TRADER_UNATTENDED=
TRADER_MAX_ORDER_NOTIONAL=2000
TRADER_MAX_ORDERS_PER_RUN=10
TRADER_DAILY_NOTIONAL_CAP=500
TRADER_WEEKLY_NOTIONAL_CAP=1000

# Crypto Scalper (crypto-scalper/) — a second, independent agent on its OWN
# separate Alpaca paper account (create a second paper account at
# https://app.alpaca.markets/paper/dashboard/overview and generate keys for
# it — do not reuse the equities keys above). See crypto-scalper/CLAUDE.md
# for the strategy and why each of these exists.
CRYPTO_APCA_API_KEY_ID=
CRYPTO_APCA_API_SECRET_KEY=
CRYPTO_APCA_BASE_URL=https://paper-api.alpaca.markets

# Same on/off pattern as TRADER_UNATTENDED above.
CRYPTO_SCALPER_UNATTENDED=
CRYPTO_MAX_ORDER_NOTIONAL=200
CRYPTO_MAX_ORDERS_PER_RUN=5
CRYPTO_DAILY_NOTIONAL_CAP=300
CRYPTO_WEEKLY_NOTIONAL_CAP=1500
CRYPTO_MAX_POSITIONS=5
CRYPTO_MAX_PER_CATEGORY=2
CRYPTO_PER_TRADE_PCT_CAP=0.10
CRYPTO_MAX_HOLD_HOURS=4
CRYPTO_PROFIT_TARGET_PCT=1.5
CRYPTO_STOP_LOSS_PCT=0.75
CRYPTO_ATR_SPIKE_MULTIPLE=2.0
CRYPTO_TARGET_RISK_PCT=1.0
CRYPTO_ATR_STOP_MULTIPLIER=6.0
CRYPTO_ATR_TP_MULTIPLIER=12.0
CRYPTO_RSI_LOOKBACK_BARS=12

# Day Trader (daytrader/) — a third strategy, deliberately sharing the
# crypto scalper's Alpaca account rather than a new one (operator's choice,
# 2026-08-07 — see daytrader/CLAUDE.md "Sharing the crypto scalper's
# Alpaca account"). In GitHub Actions these are populated from the
# CRYPTO_APCA_* secrets directly (see .github/workflows/daytrader.yml) —
# only fill these in locally if you want daytrader to point somewhere
# different for local testing. Intraday EMA9/21-cross + RSI-band equities
# strategy. NOT YET BACKTESTED — see daytrader/CLAUDE.md top banner before
# enabling the live schedule.
DAYTRADER_APCA_API_KEY_ID=
DAYTRADER_APCA_API_SECRET_KEY=
DAYTRADER_APCA_BASE_URL=https://paper-api.alpaca.markets

DAYTRADER_UNATTENDED=
DAYTRADER_FAST_EMA=9
DAYTRADER_SLOW_EMA=21
DAYTRADER_RSI_PERIOD=14
DAYTRADER_RSI_ENTRY_MIN=40
DAYTRADER_RSI_ENTRY_MAX=65
# Flat fallback only — ATR-based stop/TP (below) is what's actually used
# for new entries. See daytrader/CLAUDE.md "ATR-based stop/take-profit".
DAYTRADER_STOP_LOSS_PCT=1.0
DAYTRADER_TAKE_PROFIT_PCT=2.0
# Original spec (1% of account balance per trade) — kept for documentation
# value but no longer drives sizing; see DAYTRADER_FIXED_TRADE_NOTIONAL
# below. Deliberately NOT coupled to the ATR stop — see CLAUDE.md "Why
# position sizing stays decoupled from the ATR stop".
DAYTRADER_PER_TRADE_PCT_CAP=1.0
# Fixed per-trade sizing (changed 2026-08-18, operator's explicit choice):
# ~£100, approximated as $126 at the GBP/USD rate on this date — Alpaca
# doesn't offer GBP-denominated US equity accounts. Raises per-trade risk
# from ~1% to ~12-13% of this account's ~$1,000 balance. See CLAUDE.md
# "Fixed £100 per-trade sizing".
DAYTRADER_FIXED_TRADE_NOTIONAL=126
DAYTRADER_DAILY_DRAWDOWN_LIMIT_PCT=3.0
DAYTRADER_ATR_PERIOD=14
DAYTRADER_ATR_STOP_MULTIPLIER=1.5
DAYTRADER_ATR_TP_MULTIPLIER=3.0
DAYTRADER_ATR_SMA_PERIOD=20
DAYTRADER_MAX_ORDER_NOTIONAL=500
DAYTRADER_MAX_ORDERS_PER_RUN=5
DAYTRADER_DAILY_NOTIONAL_CAP=1000
DAYTRADER_MAX_POSITIONS=5
DAYTRADER_EOD_FLATTEN_MINUTES=10
DAYTRADER_DIGEST_LOOKBACK_MINUTES=60
